extends ../layouts/index.pug

block contenidoMain
    div(class="py-2")
        h1(class="text-4xl mb-6 font-extrabold text-center") Bienes
            span.font-normal Raices
        h2(class= "text-center font-extrabold text-2xl")= page

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.text-center.text-white= message

        if !error
            a(href="/auth/login" class="text-center text-sm font-bold text-slate-600 mt-6 block") Ya puedes iniciar sesión