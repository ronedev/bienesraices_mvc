extends ../layouts/admin.pug

block styles 
    link(rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css")

block contenidoMain
    div(class="py-2")
        h1(class="text-4xl mb-6 font-extrabold text-center") Bienes
            span.font-normal Raices
        h2(class= "text-center font-extrabold text-2xl")= page

        div(class="bg-white shadow py-8 px-4 rounded mx-auto w-full my-8 md:px-10")    
            
            form(class="space-y-6" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken)
                    
                input(type="submit" class="w-full py-2 bg-indigo-500 hover:bg-indigo-700 text-white text-sm uppercase font-semibold cursor-pointer rounded" value="Publicar propiedad")
                        
block scripts
    script(src="/js/addImage.js")